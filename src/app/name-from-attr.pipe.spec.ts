import { NameFromAttrPipe } from './name-from-attr.pipe';

describe('NameFromAttrPipe', () => {
  it('create an instance', () => {
    const pipe = new NameFromAttrPipe();
    expect(pipe).toBeTruthy();
  });
});
